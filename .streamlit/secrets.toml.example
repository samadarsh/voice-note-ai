# Streamlit Cloud: paste into App settings → Secrets (not committed)
# Local dev: use .env in the project root instead.

GROQ_API_KEY = "gsk_replace_with_your_key_from_console"
GROQ_MODEL = "llama-3.1-8b-instant"
